/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                protocol: 'https',
                hostname: 'hptjxxv2zek1ahp6.public.blob.vercel-storage.com'
            }
        ]
    }
};

export default nextConfig;
